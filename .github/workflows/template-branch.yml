---

name: GHA Specific Branch Only
description: "Generic layout for a GitHub Action on a specific branch"

on:
  push:
    branches:
      - 'jv**'

jobs:
  some-unique-job-name-here:
    runs-on: ubuntu-latest
    steps:
      - name: First Task Name Here
        run: echo This is a branch template
